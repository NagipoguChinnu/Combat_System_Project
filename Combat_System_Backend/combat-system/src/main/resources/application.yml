spring:
  datasource:
    url: jdbc:mysql://localhost:3306/combat_system
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: chinnunagipogu@gmail.com
    password: fusu skvz mehd uwsy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 9090
